# 我拆了市面上的AI记忆系统，发现一个没人解决的问题——所以我写了个新的

最近在搞多Agent系统，踩了一个很深的坑。

先说结论：**现在的AI记忆系统（不管是Mem0还是别的），只解决了一半问题。**

---

## 我遇到了什么问题

假设你有几个AI Agent各司其职：
- 一个负责数据分析
- 一个负责内容创作
- 一个负责技术排查

用户过来说了一句很普通的话：**"帮我看看这个数据，分析一下趋势。"**

就这么一句话，系统要回答三个问题：

1. **哪个Agent来接？**——数据Agent还是分析Agent？还是两个都要？
2. **用户之前聊过这个话题吗？**——有没有上下文可以继承？
3. **上次派给谁了？干得怎么样？**——有没有历史记录可以参考？

现有的记忆系统（Mem0、MemOS、Memori）只能回答第2个问题——"记住"。

第1和第3个问题，它们不管。或者说得更准确：**记忆系统和调度系统是割裂的。**

---

## 我拆了他们的方案

Mem0 确实做得很好——一次LLM调用提取事实、实体链接、多信号检索。54.5K stars不是白来的。

但它解决的是"Agent记住什么"，不是"Agent该干什么"。

在多Agent场景里，这两个问题必须一起解决。你让Agent记住了用户偏好，但不知道该派谁去执行——记住了又有什么用？

---

## 所以我自己写了一个

项目叫 **AgentMem**，核心就三个设计：

### 1. 四层记忆，按时间自然衰减

```
HOT (2小时) → WARM (24小时) → COLD (永久) → ARCHIVE (30天)
```

不是所有记忆都一样重要。刚聊完的2小时内最活跃，30天后自动归档。像人一样遗忘。

### 2. 跨通道互通

用户在webchat聊完，换到飞书/Slack/Telegram，同一个Agent能接上话。

不是"你再说一遍"，是"我知道你刚才说了什么"。

### 3. 调度+记忆闭环

```
用户请求 → 意图识别 → 派Agent → 执行 → 自动记录 → 下次优化
```

每次调度自动记下来：谁在什么通道发起了什么任务、派给了谁、干得怎么样。下次自动优先选成功率高的。

---

## 数字说话

- 17个记忆模块（提取/检索/图谱/遗忘/召回/反馈/审查……）
- 纯Python，外部依赖只有1个包（chromadb）
- 零外部API，全本地运行
- MIT协议

### 跟主流方案比

| 能力 | AgentMem | 主流方案 |
|------|----------|---------|
| 四层时间分级 | ✅ | 无分级 |
| 跨通道互通 | ✅ | 无 |
| 调度+记忆联动 | ✅ | 无 |
| 多Agent隔离 | ✅ | 部分支持 |
| 矛盾检测 | ✅ | 极少 |
| 知识图谱 | ✅ | 无 |
| 记忆反馈修正 | ✅ | 少量 |
| 零外部依赖 | ✅ | 需要云服务 |

不是要跟谁比高低——只是在真实场景里，我们发现这些能力缺一不可。

---

## 开源地址

**https://github.com/wenshuangl/agent-mem**

欢迎star、提issue、吐槽。

如果你也在搞多Agent系统，遇到了类似的问题，来聊聊。

---

*AgentMem 不是为了赶超谁而做的，而是因为真的在用它解决问题。*
