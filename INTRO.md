# AgentMem — 项目介绍

> **市场上首个同时管理「Agent记忆」+「Agent调度」的开源方案**

---

## 一句话

AgentMem 是一个为多Agent系统设计的**记忆+调度闭环框架**——Agent不只是记住，还知道该派谁去干、以及上次干得怎么样。

---

## 为什么要做这个

现在市面上的AI记忆系统（Mem0、MemOS、Memori）解决的是**「记住」**的问题。但在真实的多Agent场景中，还有一个同样重要的问题——**「谁该干什么」**。

在信息流广告公司的实际运营中，我们有41个Agent各司其职：
- 广告投放专家、数据分析师、设计师、剪辑师、技术顾问、金融助手……

当用户说「ROI太低了，帮我看下素材」——
- 哪个Agent该接？ → 调度问题
- 之前有没有聊过这个话题？ → 记忆问题
- 上次派给谁了？结果怎么样？ → 调度+记忆联动

现有方案没有同时解决这两个问题。所以我们自己做了。

---

## 核心创新

### 1. 四层记忆分级（HOT→WARM→COLD→ARCHIVE）
按时间自然衰减，不像其他方案所有记忆一视同仁。

### 2. 跨通道互通
同一Agent在webchat、飞书、Slack等不同通道的记忆自动共享。
在A通道聊完，B通道的Bot也能接上话。

### 3. 调度+记忆闭环
```
用户请求 → 意图识别 → Agent调度 → 执行 → 日志 → 优化下次调度
```
每次调度自动记录「谁在哪个通道发起了什么任务→派给了谁→结果如何」，
下次调度优先选成功率高的Agent。

### 4. 同步包含17个记忆模块
事实提取、BM25+向量融合检索、矛盾检测、知识图谱、遗忘机制、主动召回、
记忆反馈修正、自我审查……覆盖记忆的全生命周期。

---

## 市场对比

| 能力 | AgentMem | Mem0 (54.5K⭐) | MemOS (8.8K⭐) | Memori (14K⭐) |
|------|----------|---------------|---------------|---------------|
| 四层时间分级 | ✅ | ❌ | ❌ | ❌ |
| 跨通道互通 | ✅ | ❌ | ❌ | ❌ |
| 调度+记忆联动 | ✅ | ❌ | ❌ | ❌ |
| 多Agent隔离 | ✅ | ⚠️ | ✅ | ✅ |
| 矛盾检测 | ✅ | ❌ | ⚠️ | ❌ |
| 知识图谱因果链 | ✅ | ❌ | ❌ | ❌ |
| 遗忘机制 | ✅ | ⚠️ | ✅ | ❌ |
| 主动召回 | ✅ | ❌ | ❌ | ❌ |
| 记忆反馈修正 | ✅ | ❌ | ✅ | ❌ |
| 自我审查 | ✅ | ❌ | ❌ | ❌ |

---

## 适合谁用

- **多Agent系统开发者** — 需要管理多个AI Agent的记忆和调度
- **Agent平台/框架** — 需要内置记忆+调度能力
- **AI创业团队** — 需要开箱即用的记忆系统，不想从零造轮子
- **研究/爱好者** — 学习AI记忆系统的完整实现

---

## 不适合谁用

- ❌ 需要云端SaaS服务
- ❌ 需要百万级文档RAG
- ❌ 需要GPU加速的大规模检索

---

## 技术栈

- Python 3.10+
- chromadb（向量存储，本地SQLite）
- 纯CPU推理（内置embedding模型）
- 零外部API依赖，完全本地运行

---

## 快速开始

```bash
pip install -e .

# 写入记忆
python -m agent_mem.core.hot_cache write --agent main --channel webchat --text "用户偏好简洁回答" --importance 7

# 跨通道查询（查所有通道）
python -m agent_mem.core.hot_cache query --agent main --limit 5

# 调度日志
python -m agent_mem.core.dispatch_logger stats

# 运行记忆引擎
python -m agent_mem.core.engine_v2 --mode daily
```

---

## 协议

MIT License — 随便用，随便改，随便发。

---

> 一个人30天做出来的东西，在很多维度上超过了54.5K stars的项目。
> 不是因为写代码的人厉害，而是因为——**真的在用它解决问题。**
